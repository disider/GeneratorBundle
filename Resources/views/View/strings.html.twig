{% block strings %}
{% set entityName = entity_prefix|capitalize|replace('_', ' ') %}

-> messages.en.yml:

    {{ entity_prefix ~ ':'}}
        {{ 'title:'}}
            {{ 'create: Create ' ~ entityName }}
            {{ 'edit: Edit ' ~ entityName }}
            {{ 'list: List ' ~ entityName }}

    {%- for field in fields %}

    {{ 'fields:'}}
        {{ field.name ~ ': ' ~ entityName }}
    {%- endfor %}

    {{ 'flash:'}}
        {{ entity_prefix ~ ':'}}
            {{ 'created: ' ~ entityName|capitalize ~ ' successfully created.' }}
            {{ 'updated: ' ~ entityName|capitalize ~ ' successfully updated.' }}
            {{ 'deleted: ' ~ entityName|capitalize ~ ' successfully deleted.' }}

{% endblock strings %}
