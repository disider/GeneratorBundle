{% block strings %}
{% set entityName = entity_prefix|capitalize|replace('_', ' ') %}

-> messages.en.yml:

    {{ entity_prefix ~ ':'}}
        {{ 'title:'}}
            {{ 'create: Create ' ~ entityName }}
            {{ 'edit: Edit ' ~ entityName }}
            {{ 'list: List ' ~ entityName }}

    {%- for field in fields %}

    {{ 'fields.' ~ field.name ~ ': ' ~ entityName }}
    {%- endfor %}

{% endblock strings %}

{#actions.filter#}
{#actions.reset#}
{#actions.title#}
{#actions.new#}
{#actions.save#}
{#actions.save_and_close#}
