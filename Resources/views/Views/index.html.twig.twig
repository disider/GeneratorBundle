{% autoescape false %}
{% block extends %}
    {{ "{% extends '::base.html.twig' %}" }}
{% endblock extends %}

{% block body %}

{{ "{% block body %}" }}

    {{ "{% include 'DisideGeneratorBundle:Default:index.html.twig' with {
        'route_prefix': '" ~ route_prefix ~ "',
        'message_prefix': '" ~ message_prefix ~ "',
        'entities' : " ~ entity[:1]|lower ~ entity[1:]|pluralize  ~ "
        } %}" }}

{{ "{% endblock body %}" }}

{% endblock body %}
{% endautoescape %}
