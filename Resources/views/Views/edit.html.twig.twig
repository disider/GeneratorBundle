{% autoescape false %}
{% block extends %}
    {{ "{% extends '::base.html.twig' %}" }}
{% endblock extends %}

{% block body %}

{{ "{% block body %}" }}
    <h2> {{ "{{ '" ~ message_prefix ~ ".title.edit'|trans }}" }}</h2>

    {{ "{% include 'DisideGeneratorBundle:Default:_form.html.twig' with {'path': path('" ~ route_prefix ~ "_edit', {'id': form.vars.data.id})} %}" }}

{{ "{% endblock body %}" }}

{% endblock body %}
{% endautoescape %}
